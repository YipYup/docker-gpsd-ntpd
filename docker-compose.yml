version: "3"
services:
    gpsd:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 2947:2947
        restart: always
        entrypoint:
        - /bin/bash
        - -c
        - /init